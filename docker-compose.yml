# ============================================================================
# 🐳 DOCKER COMPOSE - AMBIENTE COMPLETO RESULTADOS
# ============================================================================
# 
# Orquestração completa do microserviço resultados:
# - MySQL 8.0 com R2DBC
# - Redis para cache distribuído  
# - Prometheus para métricas
# - Grafana para dashboards
# - Volumes persistentes
# - Rede isolada
# 
# Uso: docker-compose up -d
# ============================================================================

version: '3.9'

services:
  # ========================================
  # 📊 MICROSERVIÇO RESULTADOS
  # ========================================
  resultados-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: resultados-microservice
    restart: unless-stopped
    ports:
      - "8082:8082"
    environment:
      # Azure OIDC Configuration
      - AZURE_CLIENT_ID=${AZURE_CLIENT_ID}
      - AZURE_TENANT_ID=${AZURE_TENANT_ID}
      - AZURE_KEYVAULT_ENDPOINT=https://conexao-de-sorte-results-microservice.vault.azure.net/
      
      # Database (fallbacks para desenvolvimento local)
      - conexao_de_sorte_database_url=${conexao_de_sorte_database_url:-r2dbc:mysql://resultados-mysql:3306/conexao_sorte_resultados?useSSL=false&allowPublicKeyRetrieval=true}
      - conexao_de_sorte_database_username=${conexao_de_sorte_database_username:-resultados_user}
      - conexao_de_sorte_database_password=${conexao_de_sorte_database_password:-}
      
      # Redis (fallbacks para desenvolvimento local)
      - conexao_de_sorte_redis_host=${conexao_de_sorte_redis_host:-resultados-redis}
      - conexao_de_sorte_redis_port=${conexao_de_sorte_redis_port:-6379}
      - conexao_de_sorte_redis_password=${conexao_de_sorte_redis_password:-}
      
      # JWT (segredos Azure Key Vault)
      - conexao_de_sorte_jwt_jwks_uri=${conexao_de_sorte_jwt_jwks_uri:-http://autenticacao-service:8081/.well-known/jwks.json}
      - conexao_de_sorte_jwt_issuer=${conexao_de_sorte_jwt_issuer:-https://auth.conexaodesorte.com}
      - conexao_de_sorte_jwt_secret=${conexao_de_sorte_jwt_secret:-}
      - conexao_de_sorte_jwt_key_id=${conexao_de_sorte_jwt_key_id:-}
      
      # Features
      - FEATURE_RESULTADOS_MS=true
      - FEATURE_ADVANCED_RANKING=true
      - FEATURE_STATS_CACHE=true
      - FEATURE_AUDIT_LOGGING=true
      - FEATURE_METRICS=true
      
      # Observability
      - TRACING_PROBABILITY=1.0
      - LOG_LEVEL=INFO
      
      # Performance
      - JAVA_TOOL_OPTIONS=-Xms512m -Xmx1g -XX:+UseG1GC -XX:MaxGCPauseMillis=200
    depends_on:
      - resultados-mysql
      - resultados-redis
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8082/actuator/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    networks:
      - conexao-network
    volumes:
      - ./logs:/app/logs

  # ========================================
  # 🗄️ MYSQL DATABASE
  # ========================================
  # resultados-mysql:
    # image: mysql:8.4
    # container_name: resultados-mysql
    # restart: unless-stopped
    # ports:
    #   - "3307:3306"  # Porta diferente para evitar conflito
    # environment:
    #   - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD}
    #   - MYSQL_DATABASE=conexao_sorte_resultados
    #   - MYSQL_USER=resultados_user
    #   - MYSQL_PASSWORD=${DB_PASSWORD}
    #   - MYSQL_CHARACTER_SET_SERVER=utf8mb4
    #   - MYSQL_COLLATION_SERVER=utf8mb4_unicode_ci
    # command: >
    #   --character-set-server=utf8mb4
    #   --collation-server=utf8mb4_unicode_ci
    #   --default-authentication-plugin=mysql_native_password
    #   --innodb-buffer-pool-size=1G
    #   --innodb-log-file-size=256M
    #   --max-connections=200
    # volumes:
    #   - resultados-mysql-data:/var/lib/mysql
    #   - ./docker/mysql/init.sql:/docker-entrypoint-initdb.d/01-init.sql:ro
    #   - ./src/main/resources/db/migration:/docker-entrypoint-initdb.d/migrations:ro
    # networks:
    #   - conexao-network
    # healthcheck:
    #   test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
    #   timeout: 20s
    #   retries: 10

  # ========================================
  # 🚀 REDIS CACHE
  # ========================================
  resultados-redis:
    image: redis:7-alpine
    container_name: resultados-redis
    restart: unless-stopped
    ports:
      - "6380:6379"  # Porta diferente para evitar conflito
    environment:
      - REDIS_PASSWORD=${conexao_de_sorte_redis_password:-}
    command: >
      redis-server
      --requirepass ${conexao_de_sorte_redis_password:-secure-redis-password-from-keyvault}
      --maxmemory 512mb
      --maxmemory-policy allkeys-lru
      --tcp-keepalive 60
      --timeout 300
    volumes:
      - resultados-redis-data:/data
    networks:
      - conexao-network
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${conexao_de_sorte_redis_password:-secure-redis-password-from-keyvault}", "ping"]
      timeout: 20s
      retries: 5

  # ========================================
  # 📈 PROMETHEUS (Métricas)
  # ========================================
  prometheus:
    image: prom/prometheus:latest
    container_name: resultados-prometheus
    restart: unless-stopped
    ports:
      - "9091:9090"  # Porta diferente para evitar conflito
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/etc/prometheus/console_libraries'
      - '--web.console.templates=/etc/prometheus/consoles'
      - '--storage.tsdb.retention.time=200h'
      - '--web.enable-lifecycle'
    volumes:
      - ./docker/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus
    networks:
      - conexao-network
    depends_on:
      - resultados-service

  # ========================================
  # 📊 GRAFANA (Dashboards)
  # ========================================
  grafana:
    image: grafana/grafana:latest
    container_name: resultados-grafana
    restart: unless-stopped
    ports:
      - "3001:3000"  # Porta diferente para evitar conflito
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=${GF_SECURITY_ADMIN_PASSWORD}
      - GF_USERS_ALLOW_SIGN_UP=false
      - GF_INSTALL_PLUGINS=redis-datasource
    volumes:
      - grafana-data:/var/lib/grafana
      - ./docker/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./docker/grafana/dashboards:/var/lib/grafana/dashboards:ro
    networks:
      - conexao-network
    depends_on:
      - prometheus

# ========================================
# 💾 VOLUMES PERSISTENTES
# ========================================
volumes:
  resultados-mysql-data:
    driver: local
  resultados-redis-data:
    driver: local
  prometheus-data:
    driver: local
  grafana-data:
    driver: local

# ========================================
# 🌐 REDE ISOLADA
# ========================================
networks:
  conexao-network:
    driver: bridge
    ipam:
      config:
        - subnet: 172.25.0.0/16
